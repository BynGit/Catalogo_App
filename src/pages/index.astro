---
import "../styles/global.css";
import Layout from "../layouts/Layout.astro";
import Sidebar from "../components/Sidebar.astro";
import SearchBar from "../components/SearchBar.astro";
import CategoryFilter from "../components/CategoryFilter.astro";
import ProductCard from "../components/ProductCard.astro";

const response = await fetch(new URL("/api/products", Astro.url));
const products = response.ok ? await response.json() : [];
---

<Layout title="Catálogo de Productos - SGVC">
  <!-- <Sidebar /> -->
  <main class="flex-1 flex flex-col p-8 overflow-y-auto bg-white">
    <header class="mb-6">
      <h2 class="text-3xl font-bold text-gray-800 mb-6">
        Catálogo de Productos
      </h2>
      <SearchBar />
    </header>

    <section>
      <CategoryFilter />
    </section>

    <section>
      <h3 class="text-lg font-bold text-gray-800 mb-4">Productos</h3>
      <div
        class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6 pb-8"
      >
        {
          products.map((product:any) => (
            <ProductCard
              idProducto={product.idProducto}
              nombre={product.nombre}
              imagen={product.imagen}
              categoria={product.categoria}
              precio={product.precio}
            />
          ))
        }
      </div>
    </section>
  </main>
</Layout>
